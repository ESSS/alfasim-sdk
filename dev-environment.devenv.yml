name: alfasim-sdk-dev

includes:
  - {{ root }}/environment.devenv.yml

dependencies:
  - cogapp
  - doxygen
  - graphviz
  - invoke
  - pip
  - pre-commit
  - python
  - toposort
  - tox
  - typing_inspect
  - pip:
    - "-r docs/requirements.txt"
