name: alfasim-sdk


dependencies:
  - click
  - attrs >= 19.1.0
  - barril
  - h5py

environment:
  PATH:
    - {{ os.path.join(root, 'bin') }}

  PYTHONPATH:
    - {{ os.path.join(root, 'src') }}
    - {{ os.path.join(root, 'tests', 'common_testing') }}
