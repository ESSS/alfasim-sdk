[tox]
envlist = py36

[testenv]
passenv = TOXENV CI TRAVIS TRAVIS_* PYBIND_PATH  APPVEYOR APPVEYOR_*
setenv =
    PYTHONPATH = {toxinidir}
deps =
    -r{toxinidir}/requirements_dev.txt
    codecov
    git+https://git@github.com/ESSS/hookman.git

commands =
    pytest --basetemp={envtmpdir} --cov=alfasim_sdk
    codecov


